__pycache__/
*.pyc
*.pyo
*.pyd
env/
venv/

# Credentials file (contains API secrets)
credentials.txt

# MCP configuration (contains local paths)
.vscode/mcp.json

# Session data files (local only, not tracked)
runs.jsonl
sessions.jsonl

# KISS Overlay state (runtime data)
overlay_state.json
overlay_state.json.tmp

# Backup directory and files
backups/
*.backup
*.backup_*

# Debug output
debug/*.json
!debug/.gitkeep

# Cache files
cache/
.vscode/mcp.json
